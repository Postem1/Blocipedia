<h1>All Wikis</h1>

<% @wikis.each do |wiki| %>
  <div class="media">
    <div class="media-body">
      <h3 class="media-heading">
        <%= link_to markdown(wiki.title), wiki %>
      </h3>
    </div>
  </div>
<% end %>
