
<div class="jumbotron">
  <h1>Welcome to Blocipedia</h1>
  <p>A SaaS app that allows users to create their own wikis.</p>

  <% if !user_signed_in? %>
      <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-info" %>

      <%= link_to "Sign In", new_user_session_path, class: "btn btn-primary" %>
  <% end %>

  <% if user_signed_in? %>

    <% if current_user.role == 'standard' %>
    <ul><%= link_to "Upgrade to Premium", new_charge_path, class: "btn btn-primary" %></ul>
    <% end %>
    <% if current_user.role == 'premium' %>
    <ul><%= link_to "Downgrade to Standard", edit_user_registration_path(current_user), class: 'btn btn-danger' %></ul>
    <% end %>
    <br>
    <ul>
        <%= link_to "Create a new wiki", new_wiki_path, class: "btn btn-success" %>
    </ul>
    <br>
    <ul>
        <%= link_to "View all wikis", wikis_path, class: "btn btn-info" %>
    </ul>
    <br>
    <ul>
        <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-danger" %>
    </ul>
    <br>

    <% if current_user.admin? %>
    <ul>
        <%= link_to "View all users", users_path, class: "btn btn-default" %>
    </ul>
    <% end %>
  <% end %>
  <br>
</div>
